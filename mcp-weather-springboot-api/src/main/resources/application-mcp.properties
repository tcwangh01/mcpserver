# MCP 模式配置
# 使用方式: java -jar app.jar --spring.profiles.active=mcp

# 啟用 MCP 模式
mcp.mode=true

# 關閉 Web Server（使用 STDIO）
spring.main.web-application-type=none

# 日誌輸出到檔案，避免干擾 STDIO
logging.file.name=${user.home}/mcp-weather-api.log
logging.pattern.console=
